#!/usr/bin/env bash
set -euo pipefail
project="${1:-.}"
exec joern-agent compare "$project/.joern-agent/baseline.json" "$project/.joern-agent/post.json" --output "$project/.joern-agent/comparison.json" --json
